# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2  # EmailStr needs email-validator
python-multipart==0.0.12

# DB
sqlalchemy[asyncio]==2.0.35
asyncpg>=0.30  # 0.29 has no cpython 3.13 wheels; >=0.30 ships them
alembic==1.13.3

# Auth
passlib[bcrypt]==1.7.4
bcrypt<4.1  # passlib 1.7.4 is incompatible with bcrypt >= 4.1 (read-only attribute error)
python-jose[cryptography]==3.3.0

# Realtime / CRDT
pycrdt==0.10.9
websockets==13.1
# y-protocols is intentionally NOT pinned: the backend speaks its own wire format
# (binary doc updates, JSON awareness messages), and the frontend's y-protocols
# awareness layer consumes the server's presence broadcasts without needing a
# server-side dependency. See app/realtime/manager.py docstring.

# Queue
redis==5.1.1
# rq removed (audit LOW #8): never imported; the queue is redis-py direct.

# Debugger
debugpy==1.8.7

# Sandbox
docker==7.1.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
orjson==3.10.7
tenacity==9.0.0
cryptography==43.0.1

# Dev / quality
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.7.2
mypy==1.13.0
bandit==1.7.10
